name: app
description: A sample command-line application.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.5.4

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0
  api_service:
    path: ../

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
